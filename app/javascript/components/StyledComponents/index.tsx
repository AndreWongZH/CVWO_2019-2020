import styled from 'styled-components';
import { Segment } from 'semantic-ui-react';

export const SegmentMargin = styled(Segment)`
  &&& {
    margin-top: 3em;
  }
`;

export const StyledLabel = styled.div`
  margin: 0.2em;
  display: inline-block;
`;
